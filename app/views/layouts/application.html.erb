<!DOCTYPE html>
<html>
  <head>
    <title>Freelancers</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
      <nav>
      <% if professional_signed_in? %> 
          <%= current_professional.email %></li> ||
          <%= link_to 'Sair', destroy_professional_session_path, method: :delete %> 
        <div>=================================================================</div>
      <% elsif user_signed_in? %>
          <%= current_user.email %> ||
          <%= link_to 'Cadastrar um novo projeto' %>
          <%= link_to 'Meus projetos' %> ||
          <%= link_to 'Sair', destroy_user_session_path, method: :delete %> 
          <div>=================================================================</div>
      <% else %>
        <p><%= link_to 'Entrar como profissional', new_professional_session_path %> || 
        <%= link_to 'Entrar como usuário', new_user_session_path %></p>
      <% end %>
    </nav>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <%= yield %>
  </body>
</html>
